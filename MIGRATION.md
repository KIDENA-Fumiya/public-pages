# Public Pages legacy redirect repository

Session: 2026-07-27

This repository contains only compatibility redirects from the retired
`KIDENA-Fumiya` namespace to the canonical Pages repository:
`fumiya-kidena/public-pages`.

Migration decision:

- Preserve `index.html`, `404.html`, and `.nojekyll` so old links continue to
  redirect.
- Rename this repository to `public-pages-legacy` before transferring it to
  the `fumiya-kidena` personal account, because `public-pages` already exists
  there as the canonical publishing repository.
- Do not publish new content from this repository.
